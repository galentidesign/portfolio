slug: widget
name: Widget
tier: hero
status: stable
description: Missing the required tokens key.

variants:
  variant: [default]

props:
  - name: variant
    type: "'default'"
    description: The variant.

# tokens key intentionally absent

a11y:
  keyboard:
    - keys: Enter
      does: Activates.
  aria:
    - Provides accessible name.
  contrast: Uses accent pair.

usage:
  do:
    - Use for main actions.
  dont:
    - Do not nest.

example: |
  <Widget />

links:
  repo: app/frontend/ds/components/Widget
  figma: null
