slug: widget
name: Widget
tier: hero
status: stable
description: Has a malformed a11y.keyboard entry missing the required does field.

variants:
  variant: [default]

props:
  - name: variant
    type: "'default'"
    description: The variant.

tokens:
  - --color-accent

a11y:
  keyboard:
    - keys: Enter
  aria:
    - Provides accessible name.
  contrast: Uses accent pair.

usage:
  do:
    - Use for main actions.
  dont:
    - Do not nest.

example: |
  <Widget />

links:
  repo: app/frontend/ds/components/Widget
  figma: null
