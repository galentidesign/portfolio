slug: widget
name: Widget
tier: hero
status: stable
description: Has a links.repo that does not start with the required path prefix.

variants:
  variant: [default]

props:
  - name: variant
    type: "'default'"
    description: The variant.

tokens:
  - --color-accent

a11y:
  keyboard:
    - keys: Enter
      does: Activates.
  aria:
    - Provides accessible name.
  contrast: Uses accent pair.

usage:
  do:
    - Use for main actions.
  dont:
    - Do not nest.

example: |
  <Widget />

links:
  repo: src/components/Widget
  figma: null
