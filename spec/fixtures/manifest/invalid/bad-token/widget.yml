slug: widget
name: Widget
tier: hero
status: stable
description: Contains a token with a non-semantic prefix.

variants:
  variant: [default]

props:
  - name: variant
    type: "'default'"
    description: The variant.

tokens:
  - --color-accent
  - --custom-primary

a11y:
  keyboard:
    - keys: Enter
      does: Activates.
  aria:
    - Provides accessible name.
  contrast: Uses accent pair.

usage:
  do:
    - Use for main actions.
  dont:
    - Do not nest.

example: |
  <Widget />

links:
  repo: app/frontend/ds/components/Widget
  figma: null
