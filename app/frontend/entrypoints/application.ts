// Global entrypoint: token/skin CSS (compiled from ds/tokens sources) and the
// app-shell floor load on every page, independent of the React/Inertia mount.
import '../ds/tokens/generated/index.css'
import '../shell/global.css'
